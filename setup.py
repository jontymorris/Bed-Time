from setuptools import setup, find_packages

setup(
    name="BedtimeProject",
    decription="Shuts down Windows when it is bedtime",
    author="Jonty Morris",
    url="github.com/jontymorris/bed-time",
    packages=find_packages()
)